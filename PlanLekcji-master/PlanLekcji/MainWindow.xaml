<Window x:Class="PlanLekcji.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlanLekcji"
        mc:Ignorable="d"
        Title="Plan lekcji" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="SubjectsListBox"
                 ItemsSource="{Binding Subjects}"
                 Grid.Row="0"
                 SelectionChanged="SubjectsListBox_SelectionChanged"
                 FontSize="14"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,8,0,0">
            <TextBox x:Name="SubjectTextBox"
                     Width="320"
                     VerticalAlignment="Center"
                     KeyDown="SubjectTextBox_KeyDown"
                     />
            <Button x:Name="AddButton" Content="Dodaj" Margin="8,0,0,0" Click="AddButton_Click" />
            <Button x:Name="RemoveButton" Content="Usuń" Margin="8,0,0,0" Click="RemoveButton_Click" IsEnabled="False" />
        </StackPanel>
    </Grid>
</Window>
